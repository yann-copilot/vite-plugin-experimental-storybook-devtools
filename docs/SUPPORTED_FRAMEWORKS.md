# Supported Frameworks

Keep this file up to date whenever framework integrations are added, removed, or significantly changed.

Current integrations:

- React (`src/frameworks/react`)
- Vue (`src/frameworks/vue`)
